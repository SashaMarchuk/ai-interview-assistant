---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - wxt.config.ts
  - tsconfig.json
  - tailwind.config.ts
  - src/assets/app.css
  - public/icon/icon-16.png
  - public/icon/icon-32.png
  - public/icon/icon-48.png
  - public/icon/icon-128.png
autonomous: true

must_haves:
  truths:
    - "npm run dev starts WXT development server without errors"
    - "TypeScript compiles with strict mode enabled"
    - "Tailwind CSS processes utility classes"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "wxt"
    - path: "wxt.config.ts"
      provides: "WXT configuration with manifest"
      contains: "defineConfig"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
  key_links:
    - from: "wxt.config.ts"
      to: "package.json"
      via: "wxt module resolution"
      pattern: "@wxt-dev/module-react"
---

<objective>
Initialize WXT-based Chrome MV3 extension project with React, TypeScript, and Tailwind CSS.

Purpose: Create the build infrastructure that all other Phase 1 plans depend on.
Output: Working development environment with `npm run dev` launching WXT dev server.
</objective>

<execution_context>
@/Users/sasha-marchuk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sasha-marchuk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize WXT project with React template</name>
  <files>
    package.json
    wxt.config.ts
    tsconfig.json
  </files>
  <action>
    Create a new WXT project using the React template. Run:

    ```bash
    npm create wxt@latest . -- --template react
    ```

    If prompted about existing files, allow overwrite. This creates the base project structure.

    After initialization, verify package.json has:
    - wxt
    - @wxt-dev/module-react
    - react, react-dom
    - typescript

    Update wxt.config.ts with the manifest configuration from research:
    - name: "AI Interview Assistant"
    - version: "0.1.0"
    - permissions: tabCapture, activeTab, offscreen, storage, scripting
    - host_permissions: https://meet.google.com/*
    - content_security_policy with connect-src for ElevenLabs and OpenRouter
    - minimum_chrome_version: "116"

    Ensure tsconfig.json has:
    - strict: true
    - path alias "@/*" pointing to "./src/*"
  </action>
  <verify>
    Run `npm install` followed by `npm run dev` - WXT dev server should start.
    Run `npx tsc --noEmit` - TypeScript should compile without errors.
  </verify>
  <done>
    WXT dev server starts successfully with React module loaded.
    TypeScript compiles in strict mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4</name>
  <files>
    tailwind.config.ts
    src/assets/app.css
    package.json
  </files>
  <action>
    Install Tailwind CSS v4 and its Vite plugin:

    ```bash
    npm install -D tailwindcss @tailwindcss/vite
    ```

    Create src/assets/app.css with Tailwind v4 import:
    ```css
    @import "tailwindcss";
    ```

    Update wxt.config.ts to add the Tailwind Vite plugin:
    ```typescript
    import tailwindcss from '@tailwindcss/vite';

    export default defineConfig({
      vite: () => ({
        plugins: [tailwindcss()],
      }),
      // ... rest of config
    });
    ```

    Note: Tailwind v4 uses CSS-based configuration by default. No tailwind.config.ts needed unless customizing.
  </action>
  <verify>
    Add a Tailwind class (e.g., `className="text-blue-500"`) to the popup component.
    Run `npm run dev` and verify styles are applied in the popup.
  </verify>
  <done>
    Tailwind utility classes work in popup component.
    No build errors related to CSS processing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder extension icons</name>
  <files>
    public/icon/icon-16.png
    public/icon/icon-32.png
    public/icon/icon-48.png
    public/icon/icon-128.png
  </files>
  <action>
    Create the public/icon/ directory and generate simple placeholder icons.

    Use a Node.js script or download placeholder PNGs. For quick placeholder:

    Option A - Use ImageMagick if available:
    ```bash
    mkdir -p public/icon
    convert -size 16x16 xc:#3B82F6 public/icon/icon-16.png
    convert -size 32x32 xc:#3B82F6 public/icon/icon-32.png
    convert -size 48x48 xc:#3B82F6 public/icon/icon-48.png
    convert -size 128x128 xc:#3B82F6 public/icon/icon-128.png
    ```

    Option B - Use Node.js with sharp (install if needed):
    ```bash
    npm install -D sharp
    ```
    Then create a script that generates solid color PNGs.

    Option C - Create minimal valid PNGs programmatically using pure Node.js Buffer manipulation (PNG header + IDAT chunk for solid color).

    The icons just need to be valid PNGs for development. They'll be replaced with proper branding later.

    Update wxt.config.ts manifest.icons if WXT doesn't auto-detect:
    ```typescript
    icons: {
      16: 'icon/icon-16.png',
      32: 'icon/icon-32.png',
      48: 'icon/icon-48.png',
      128: 'icon/icon-128.png',
    },
    ```
  </action>
  <verify>
    `ls public/icon/` shows 4 PNG files.
    `npm run build` completes without icon-related errors.
  </verify>
  <done>
    Extension icons exist at all required sizes.
    WXT build includes icons in manifest.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts WXT dev server without errors
2. `npm run build` creates .output/ directory with valid extension
3. TypeScript compiles in strict mode: `npx tsc --noEmit` returns 0
4. Extension can be loaded in Chrome via "Load unpacked" (tested in Plan 03)
</verification>

<success_criteria>
- WXT development server runs without errors
- TypeScript strict mode enabled and compiling
- Tailwind CSS processing works
- Extension icons exist for all sizes
- Ready for component development in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
